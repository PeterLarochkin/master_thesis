description:  "Master thesis"
name:         "thesis"
opam-version: "2.0"


license:      "GPL-3.0-or-later"

depends: [
  "coq"           { >= "8.16.0" }
  "dune"          { >= "2.8.0"  }
]

pin-depends: [
]

build: [ "dune" "build" "-p" name "-j" jobs ]